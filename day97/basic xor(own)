   vector<int> getXor(vector<int> A, int N)
    {
        // Write your Code here
        int res{};
       for(int i=0;i<N;i++){
            res^=A[i];
       }
       for(int i=0;i<N;i++){
           A[i]^=res;
       }
        return A;
    }
};
